# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
.coverage.*
coverage.xml
# Written by the CI coverage jobs (COVERAGE_FILE=coverage-data-*). Deliberately not
# dot-prefixed, because upload-artifact skips hidden files — so the patterns above
# do not cover them and a local reproduction of those jobs would leave them untracked.
coverage-data-*
htmlcov/
.benchmarks/

# Virtual env
.venv/
.venv-*/
venv/

# Data & artifacts (do not commit audio or cached embeddings)
data/raw/
data/cache/
outputs/
*.npy
*.parquet
*.html

# OS / editor
.DS_Store
.idea/
.vscode/

# Local Claude Code settings (machine-specific)
.claude/settings.local.json

# Internal planning/roadmap notes — not part of the published OSS repo
plans/
